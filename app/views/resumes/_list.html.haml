%table
  - cols = %w(name email skills)
  %thead
    %tr
      - cols.each do |col|
        %th{ :class => col }= t("attributes.#{col}")
  %tbody
    - if @resumes.empty?
      %tr
        %td.empty{ colspan: cols.size}>= t('resumes.none')
    - @resumes.each do |res|
      %tr
        %td.name= link_to res.name, res
        %td.email= mail_to res.email
        %td.skills<
          - res.skills.each do |s|
            %span.itag<= s
