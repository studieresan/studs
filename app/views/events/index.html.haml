.container
  .row
    .col-sm-8.col-sm-offset-2.col-xs-12
      %h2= t '.upcoming_events'
      = render partial: 'table', locals: {events: @upcoming_events}
      %h2= t '.past_events'
      = render partial: 'table', locals: {events: @past_events}
      %br
      - if can? :create, Event
        = link_to 'New Event', new_event_path
