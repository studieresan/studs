- active_menu :users

.dialog.delete
  %h2= t_model_action()
  %p= t('actions.delete_prompt_html', type: t('models.the.user'), name: @user.to_s)
  .actions
    = action_link :cancel, users_path
    = button_to t('actions.delete'), @user, method: :delete , :class => :destroy
